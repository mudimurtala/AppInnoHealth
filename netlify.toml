# Netlify deployment configuration
[build]
  command = "npm run build"  # runs Vite production build
  publish = "build"          # publishes Vite output folder

[build.environment]
  NODE_VERSION = "20"        # pinned for reproducible builds across environments
